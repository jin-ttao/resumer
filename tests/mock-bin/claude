#!/bin/bash
# Mock claude binary for cc-resume QA.
# Records invocation (pwd + args) into a log, then exits.
LOG="${CLAUDE_MOCK_LOG:-/tmp/resumer-qa/claude-mock.log}"
mkdir -p "$(dirname "$LOG")"
echo "CALLED pwd=$PWD args=$*" > "$LOG"
sleep 0.2
