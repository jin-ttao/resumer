#!/bin/bash
# Mock gemini binary for resumer QA.
# Records invocation (pwd + args) into a log, then exits.
LOG="${GEMINI_MOCK_LOG:-/tmp/resumer-qa/gemini-mock.log}"
mkdir -p "$(dirname "$LOG")"
echo "CALLED pwd=$PWD args=$*" > "$LOG"
sleep 0.2