#!/usr/bin/env python3
"""resumer — unified AI CLI session resumer entry point."""
from __future__ import annotations

import os
import sys

_HERE = os.path.dirname(os.path.realpath(__file__))
_SRC = os.path.abspath(os.path.join(_HERE, "..", "src"))
if _SRC not in sys.path:
    sys.path.insert(0, _SRC)


def main() -> int:
    # Phase 5에서 argparse + dispatch 구현.
    print("resumer: scaffold only. Implementation lands in Phase 5.", file=sys.stderr)
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
